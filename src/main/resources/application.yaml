spring:
  application:
    name: SpringBoot

  datasource:
    url: jdbc:mysql://localhost:3306/studyspringboot?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 0805
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update